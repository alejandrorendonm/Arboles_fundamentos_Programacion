import java.util.Scanner;

class main {
    // Clase Nodo - cada cajita del árbol
    static class Nodo {
        int valor;
        Nodo izquierda;
        Nodo derecha;

        Nodo(int valor) {
            this.valor = valor;
            this.izquierda = null;
            this.derecha = null;
        }
    }

    // La raíz del árbol
    static Nodo raiz = null;
    static int contador = 0;  // Para contar cuántos números hay

    // INSERTAR un número en el árbol
    public static void insertar(int numero) {
        if (raiz == null) {
            raiz = new Nodo(numero);
            contador++;
            System.out.println("✓ Se insertó " + numero + " como raíz del árbol");
        } else {
            insertarNodo(raiz, numero);
        }
    }

    // Función auxiliar para insertar
    static void insertarNodo(Nodo nodo, int numero) {
        // Si el número es menor, va a la izquierda
        if (numero < nodo.valor) {
            if (nodo.izquierda == null) {
                nodo.izquierda = new Nodo(numero);
                contador++;
                System.out.println("✓ Se insertó " + numero + " a la izquierda de " + nodo.valor);
            } else {
                insertarNodo(nodo.izquierda, numero);
            }
        }
        // Si el número es mayor, va a la derecha
        else if (numero > nodo.valor) {
            if (nodo.derecha == null) {
                nodo.derecha = new Nodo(numero);
                contador++;
                System.out.println(" Se insertó " + numero + " a la derecha de " + nodo.valor);
            } else {
                insertarNodo(nodo.derecha, numero);
            }
        }
        // Si el número ya existe
        else {
            System.out.println(" El número " + numero + " ya existe en el árbol");
        }
    }

    // BUSCAR un número en el árbol
    public static void buscar(int numero) {
        System.out.print("Buscando " + numero + "... ");
        if (buscarNodo(raiz, numero)) {
            System.out.println("¡ENCONTRADO! ✓");
        } else {
            System.out.println("No está en el árbol ✗");
        }
    }

    // Función auxiliar para buscar
    static boolean buscarNodo(Nodo nodo, int numero) {
        if (nodo == null) {
            return false;
        }
        if (numero == nodo.valor) {
            return true;
        }
        if (numero < nodo.valor) {
            return buscarNodo(nodo.izquierda, numero);
        } else {
            return buscarNodo(nodo.derecha, numero);
        }
    }

    // MOSTRAR todos los números (en orden)
    public static void mostrar() {
        if (raiz == null) {
            System.out.println("El árbol está vacío");
        } else {
            System.out.println("\n=== CONTENIDO DEL ÁRBOL ===");
            System.out.println("Total de números: " + contador);
            System.out.print("Números ordenados: ");
            mostrarEnOrden(raiz);
            System.out.println("\n===========================");
        }
    }

    // Función para mostrar
    static void mostrarEnOrden(Nodo nodo) {
        if (nodo != null) {
            mostrarEnOrden(nodo.izquierda);
            System.out.print(nodo.valor + " ");
            mostrarEnOrden(nodo.derecha);
        }
    }

    // PROGRAMA PRINCIPAL
    public static void main(String[] args) {
        Scanner entrada = new Scanner(System.in);


        System.out.println("ÁRBOL BINARIO SIMPLE");

        while (true) {
            System.out.println("\n--- MENÚ ---");
            System.out.println("1. Insertar varios números");
            System.out.println("2. Buscar varios números");
            System.out.println("3. Ver árbol completo");
            System.out.println("4. Salir");
            System.out.print("Elige: ");

            int opcion = entrada.nextInt();

            if (opcion == 1) {
                // INSERTAR VARIOS NÚMEROS SIN VOLVER AL MENÚ
                System.out.println("\n=== INSERTAR NÚMEROS ===");
                System.out.println("(Escribe 0 para volver al menú)");

                while (true) {
                    System.out.print("Insertar número: ");
                    int num = entrada.nextInt();

                    if (num == 0) {
                        System.out.println("Volviendo al menú...");
                        break;
                    }

                    insertar(num);
                }
            }
            else if (opcion == 2) {
                // BUSCAR VARIOS NÚMEROS SIN VOLVER AL MENÚ
                if (raiz == null) {
                    System.out.println("⚠ El árbol está vacío, inserta números primero");
                } else {
                    System.out.println("\n=== BUSCAR NÚMEROS ===");
                    System.out.println("(Escribe 0 para volver al menú)");

                    while (true) {
                        System.out.print("Buscar número: ");
                        int num = entrada.nextInt();

                        if (num == 0) {
                            System.out.println("Volviendo al menú...");
                            break;
                        }

                        buscar(num);
                    }
                }
            }
            else if (opcion == 3) {
                mostrar();
            }
            else if (opcion == 4) {
                System.out.println("\n¡Gracias por practicar! 👋");
                System.out.println("Insertaste " + contador + " números en total");
                break;
            }
            else {
                System.out.println("⚠ Opción no válida (1-4)");
            }
        }

        entrada.close();
    }
}
